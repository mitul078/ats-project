events {}

http {
    upstream ats_api{
        server api:4000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://ats_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}